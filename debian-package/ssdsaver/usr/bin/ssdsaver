#!/usr/bin/python3
import sys
import os

# Add the application directory to Python path
sys.path.insert(0, '/usr/share/ssdsaver')

# Import and run the main application
from main import main

if __name__ == '__main__':
    sys.exit(main())
